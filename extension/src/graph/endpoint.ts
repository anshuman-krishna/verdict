// same pattern as reputation/endpoint.ts: a plausible production url under
// the real domain, not yet live. Swap it for the real deployed host if it
// ever differs from verdict.tools.
export const DEFAULT_GRAPH_CONTRIBUTION_ENDPOINT = "https://api.verdict.tools/v1/graph/contribute";
