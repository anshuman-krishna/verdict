{
  "name": "extension",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "wxt",
    "build": "wxt prepare && tsc --noEmit && wxt build && wxt build -b firefox",
    "test": "vitest run --passWithNoTests"
  },
  "devDependencies": {
    "@types/chrome": "0.2.8",
    "ajv": "8.20.0",
    "ajv-formats": "3.0.1",
    "fake-indexeddb": "6.2.5",
    "happy-dom": "20.14.0",
    "typescript": "7.0.2",
    "vitest": "5.0.0",
    "wxt": "0.21.4"
  }
}
